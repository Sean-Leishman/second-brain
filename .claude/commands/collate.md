---
description: Gather material for a Base Note I'm about to write (Curate mode)
argument-hint: <topic>
---

Run the **Collate** operation as defined in `CLAUDE.md`. This is **Curate mode** — I'm writing the Base Note, you're proposing what feeds in.

Topic: $ARGUMENTS

Steps:
1. Search across `01 Fleeting Notes/`, `02 Sources/`, `06 Daily Notes/`, and existing `05 Base Notes/` for material relevant to the topic.
2. Group findings into clusters: *direct evidence*, *adjacent concepts*, *contradictions with prior wiki claims*, *open threads*.
3. Propose one of: **new Base Note** (suggested title + skeleton), **extend existing Base Note** (which section to add), **split existing Base Note** (when one note now covers two distinct ideas), **merge two Base Notes**.
4. Cite every suggestion with `[[wikilinks]]` so I can verify before deciding.
5. **No file writes during this op.** I pick the direction; then you polish, cross-link, and update `04 Indexes/Wiki Index.md` after I've drafted.

If the topic is personal (about me), default voice is Curate — I write in first person, you stay observational in your suggestions.
