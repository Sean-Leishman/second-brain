---
description: You write the Base Note from collated material (Draft mode)
argument-hint: <topic>
---

Run the **Collate** operation as defined in `CLAUDE.md`, then immediately drop into **Draft mode** — you write the Base Note, I review and edit.

Topic: $ARGUMENTS

Steps:
1. Collate first: search `01 Fleeting Notes/`, `02 Sources/`, `06 Daily Notes/`, `05 Base Notes/` for relevant material. Briefly summarise the clusters you found.
2. Draft the Base Note in `05 Base Notes/<Title>.md` using the Base template's frontmatter conventions. Required fields: `title`, `created`, `tags`, `visibility`, `summary`, `links`, `sources` (list every source note that contributed).
3. **Cite every load-bearing claim** with `[[Source Note]]` links inline. Untraceable claims get cut, not paraphrased.
4. Show me the draft before saving anything outside the Base Note itself. Wait for my edits.
5. After I accept: update `04 Indexes/Wiki Index.md`, link from any relevant MOC, append to `04 Indexes/Wiki Log.md`.

If the topic is personal (about me), use observational voice — *"Daily Notes show X across N captures"* — not first-person assertions. I'll convert to first-person on review.
